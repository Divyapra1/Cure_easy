<!DOCTYPE html>
<html>
<head>
  <title>Book Appointment</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }
    .appointment-container {
       /* Adjusted margin-top value */
      display: flex;
      flex-direction: column;
      align-items: center;
      border: 4px solid blue;
      width: 60%;
      margin: 0 auto; /* Removed duplicate margin rule */
      padding: 20px;
      margin-top: 180px;
    }
    .form-label {
      font-size: 2.2em;
      margin-bottom: 5px;
    }
    .form-field {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 2px solid #ccc;
      border-radius: 5px;
    }
    .form-submit {
      margin-top: 20px;
      display: flex;
      justify-content: center;
        font-size: 20px; /* Adjust the font size as needed */
        padding: 10px 20px;
    }
    error-message {
  color: red;
  font-size: 14px;
  margin-top: 5px;
}
   .btnx{
    max-width: 20%;
    height: 35px;
   }
  </style>
</head>
<body style="background-color: #F8EFBA;">
  <% cat= SubCategory.find(@disc.sub_category_id)%>
<%= link_to"Back",  :back  %>

  <div class="appointment-container">
   
    <h1>Book Appointment</h1>
    <%= form_with model: @appointment, url: create_appointment_path(@user),method: :post do |form| %>
    
      <div class="form-label" style="margin-top: 50px;">
        <%= form.label :select_date, 'Select Date' %>
      </div>
      <div class="form-field">
        <%= form.date_field :select_date %>
      </div>
       <% if @appointment.errors[:select_date].any? %>
        <div class="flash-alert">
          <%= @appointment.errors[:select_date].first %>
        </div>
      <% end %>
     
      <!-- <div class="form-field">
        <%#= form.hidden_field :catalogue_id, value: params[:disc_id]%>
      </div> -->
      <div class="form-label" style="margin-top: 50px;">
        <%= form.label :select_time, 'Select Time' %>
      </div>
      <div class="form-field">
        <%= form.time_field :select_time %>
      </div>
       <% if @appointment.errors[:select_time].any? %>
        <div class="flash-alert">
          <%= @appointment.errors[:select_time].first %>
        </div>
      <% end %>
     
  </div>
      <div class="form-submit" style="margin-top: 50px ; ">
        <%= form.submit value: 'Proceed', class: 'btnx' %>
      </div>
    <% end %>
  </div>
</body>
</html>
